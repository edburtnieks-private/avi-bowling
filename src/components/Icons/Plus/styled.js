import styled from 'styled-components'

const Wrapper = styled.div`
  position: relative;
`

const Vertical = styled.div`
  position: absolute;
  top: 0;
  transform: rotate(90deg);
`

export { Wrapper, Vertical }
