import styled from 'styled-components'

import { Section } from '../../../../components/Section/styled'

const TitleSection = styled(Section)`
  @media (min-width: 1024px) {
    margin: var(--s-0) var(--s-0) var(--s-0) 50%;
  }
`

export { TitleSection }
